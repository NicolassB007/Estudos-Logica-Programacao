// Exercicios gerados pelo Google Gemini

#include <stdio.h>

int main() {
    double base, altura, area_Triangulo;

    printf("Informe a base do triangulo: ");
    scanf("%lf", &base);
    printf("Informe a altura do triangulo: ");
    scanf("%lf", &altura);

    area_Triangulo = base * altura;

    printf("AREA DO TRIANGULO = %.2lf", area_Triangulo);


    return 0;
}